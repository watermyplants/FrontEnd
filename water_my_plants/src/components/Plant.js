import React from 'react';
import { PlantList } from './PlantList';

const Plant = (props) => {

    return (
        <div>
            {console.log(props)}
        </div>
    )
}

export default Plant;